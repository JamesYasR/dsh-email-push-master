# Security

## SMTP authorization code

`config.json` holds your SMTP authorization code and is **gitignored** — never commit it or paste it into chat/issues. A leaked code lets anyone send mail as your mailbox.

## Reporting a vulnerability

Open a private issue or contact the maintainer. Never disclose secrets in public issues.
